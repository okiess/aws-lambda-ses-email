{
  "name": "aws-lambda-ses-email",
  "version": "0.0.1",
  "description": "AWS Lambda function to send an email message via AWS SES",
  "bin": {
    "node-lambda": "./bin/node-lambda"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/okiess/aws-lambda-ses-email.git"
  },
  "author": "okiess",
  "dependencies": {
    "aws-sdk": "^2.0.28",
    "commander": "^2.5.0",
    "dotenv": "^0.4.0"
  }
}
